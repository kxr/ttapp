version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/ttapp
permissions:
  - object: /var/www/ttapp/app/logs
    owner: nginx
    group: nginx
    mode: 644
    type:
      - directory
  - object: /var/www/ttapp/app/cache
    owner: nginx
    group: nginx
    mode: 644
    type:
      - directory
